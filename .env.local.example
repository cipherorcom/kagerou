# 本地开发环境配置（可选）
# 如果你想覆盖 .env 中的某些配置，可以创建 .env.local 文件

# 例如：使用不同的数据库
# DATABASE_URL="postgresql://subdomain:subdomain123@localhost:5433/subdomain_dev"

# 例如：使用不同的 Redis
# REDIS_URL="redis://:mypassword@localhost:6380"

# 例如：使用远程 API
# NEXT_PUBLIC_API_URL="http://192.168.1.100:3001"
