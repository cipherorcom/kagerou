# 数据库设置说明

## 🗄️ 数据库架构

Kagerou 使用 PostgreSQL 数据库，采用 Prisma ORM 管理数据结构。

### 数据库初始化方式

**全新项目，无迁移历史**
- 使用 `prisma db push` 直接创建数据库结构
- 使用 `scripts/init-database.js` 初始化基础数据
- 无需维护迁移文件，简化部署

### 自动化流程

Docker 容器启动时会自动：

1. **等待数据库连接**
2. **创建数据库结构**: `npx prisma db push`
3. **初始化基础数据**: `node scripts/init-database.js`

### 基础数据包含

- **DNS Providers**: Cloudflare、阿里云配置
- **系统设置**: 默认配额、限流、注册设置
- **禁用子域名**: admin、api、www 等系统保留域名

### 手动操作（如需要）

```bash
# 进入容器
docker-compose exec app sh

# 重新创建数据库结构
su kagerou -c "cd /app/packages/database && npx prisma db push"

# 重新初始化基础数据
su kagerou -c "cd /app && node scripts/init-database.js"
```

### 优势

- ✅ 无迁移历史，结构清晰
- ✅ 自动化初始化，用户无需操作
- ✅ 幂等操作，重复运行无问题
- ✅ 适合全新项目的简洁方式